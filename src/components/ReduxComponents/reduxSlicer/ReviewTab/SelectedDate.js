import { createSlice } from "@reduxjs/toolkit";

export const SelectedDate = createSlice({
  name: "SelectedDate",
  initialState: { SelectedDate: 0 },
  reducers: {},
});

export const {} = SelectedDate.actions;

export default SelectedDate.reducer;
